{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for Voxta settings and recording overlay windows.",
  "windows": ["settings", "overlay"],
  "permissions": [
    "core:default",
    "opener:default",
    "autostart:default"
  ]
}
